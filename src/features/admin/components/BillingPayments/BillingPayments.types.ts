// Placeholder types for AdminBillingPayments
export interface BillingData {
  id: string;
  amount: number;
  status: string;
  date: string;
}

export interface BillingPaymentsProps {
  // Add props as needed
}
