// Placeholder utilities for AdminMessOwnerManagement
export const formatOwnerName = (name: string): string => {
  return name.charAt(0).toUpperCase() + name.slice(1);
};

export const getOwnerStatus = (status: string): string => {
  return status.charAt(0).toUpperCase() + status.slice(1);
};

